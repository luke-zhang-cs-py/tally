[run]
omit =
    tests/*
    # Developer scripts, not shipped code.
    tools/*

[report]
exclude_lines =
    pragma: no cover
    # The script entry point starts a server and blocks, so no test can run
    # it; it is verified by running the app.
    if __name__ == .__main__.:
